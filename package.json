{
  "name": "jbc-athenaeum-php",
  "private": true,
  "version": "1.0.0",
  "description": "JBC ATHENAEUM implemented with PHP, HTML, and pure CSS.",
  "scripts": {
    "lint": "php -l index.php && php -l includes/data.php",
    "build": "echo 'No build step required for PHP/HTML project.'"
  }
}
